{
  "name": "@subroutinecom/workspace",
  "version": "0.1.7",
  "description": "Self-contained CLI for spinning up Docker-in-Docker workspaces with SSH and proxy helpers.",
  "bin": {
    "workspace": "./src/index.js"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "commander": "^11.1.0",
    "fs-extra": "^11.2.0",
    "ora": "^5.4.1",
    "proper-lockfile": "^4.1.2",
    "update-notifier": "^7.3.1",
    "yaml": "^2.3.4"
  },
  "devDependencies": {
    "vitest": "^4.0.6"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/subroutinecom/workspace.git"
  },
  "keywords": [
    "docker",
    "development",
    "workspace",
    "cli",
    "docker-in-docker",
    "ssh"
  ],
  "author": "Subroutine",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  }
}
